name: hotel_app
description: "AALA Hotel POS System"
publish_to: 'none'

version: 1.0.2+3

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.3.4
  provider: ^6.1.1
  intl: ^0.19.0
  pdf: ^3.10.7
  printing: ^5.12.0
  ota_update: ^5.0.1
  http: ^1.2.0
  print_bluetooth_thermal: ^1.1.1
  flutter_pos_printer_platform: ^1.0.8
  esc_pos_utils_plus: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
