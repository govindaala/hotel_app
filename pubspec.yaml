name: hotel_pos
description: "Zero Cost Offline First Hotel POS and Supply System"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  shared_preferences: ^2.2.3
  http: ^1.2.0
  print_bluetooth_thermal: ^1.1.2
  esc_pos_utils_plus: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
